Add lvm support
Add support for: efs, exofs, freevxfs, nilfs2, omfs, ufs
Add raid support
Add dmcrypt support
Several file systems have size limits. Fix this by passing a range in.
add fsck as a test in run_test.
Fix ext3 so that it gets a file of the right size. Some small ones do not work.
